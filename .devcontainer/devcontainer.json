{
  "//": "cuberhaus reproducible dev environment (latex). Uses the full TeX Live image (latexmk, biber, etc.). Managed source: cuberhaus-workspace/assets/devcontainers/latex. Applied per repo by scripts/apply-devcontainers.ps1.",
  "name": "cuberhaus latex",
  "image": "texlive/texlive:latest",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "bash -lc 'if [ -f skills-lock.json ]; then make skills-restore || true; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "James-Yu.latex-workshop"
      ]
    }
  }
}
